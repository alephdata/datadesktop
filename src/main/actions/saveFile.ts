import { BrowserWindow, app, dialog } from 'electron'
import {SAVE_FILE_SUCCESS} from '../actionTypes'


export const saveFile = () => {
  // storage.set(key, data, (error) => {
  //   if (error) throw error;
  //   win.webContents.send(SAVE_FILE_SUCCESS);
  // });
}
